{# Shared User delete form (Admin) #}
{% set delete_route = delete_route|default('admin_user_delete') %}
{% set delete_params = delete_params|default({id: user.id}) %}
{% set confirm_message = confirm_message|default("Confirmer la suppression ?") %}

<form method="post"
      action="{{ path(delete_route, delete_params) }}"
      onsubmit="return confirm('{{ confirm_message|e('js') }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-outline-danger w-100">Supprimer</button>
</form>
