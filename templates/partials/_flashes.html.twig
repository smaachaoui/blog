{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} shadow-sm" role="alert">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
