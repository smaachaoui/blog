{% extends 'base.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
<div class="card shadow-sm page-card">
    <div class="card-body p-4">
        <h1 class="h4 mb-3">Administration</h1>

        <ul class="mb-0">
            <li><a href="{{ path('admin_category_index') }}">Catégories</a></li>
            <li><a href="{{ path('admin_post_index') }}">Articles</a></li>
            <li><a href="{{ path('admin_comment_index', {pending: 1}) }}">Commentaires (en attente)</a></li>
        </ul>
    </div>
</div>
{% endblock %}
