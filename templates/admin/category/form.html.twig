{# J'ai cree le template de base pour les formulaires categorie admin #}
{% extends 'admin/base.html.twig' %}

{% block admin_title %}{{ page_title|default('Catégorie') }}{% endblock %}

{% block admin_content %}
{% include 'components/_page_header.html.twig' with {
    title: heading|default(page_title|default('Catégorie')),
    subtitle: subtitle|default('Gérer le nom et le slug.'),
    back_route: 'admin_category_index',
    back_label: 'Retour à la liste'
} %}

<div class="row g-4">
    <div class="col-12 col-lg-8">
        <div class="card shadow-sm">
            <div class="card-body p-4">
                {% include 'form/_category.html.twig' with {
                    form: form,
                    button_label: button_label|default('Enregistrer'),
                    cancel_route: 'admin_category_index'
                } %}
            </div>
        </div>
    </div>

    {% if show_delete|default(false) and category is defined %}
        <div class="col-12 col-lg-4">
            {% include 'components/_danger_zone.html.twig' with {
                delete_route: 'admin_category_delete',
                delete_params: {id: category.id},
                entity_id: category.id,
                confirm_message: 'Supprimer cette catégorie ?'
            } %}
        </div>
    {% endif %}
</div>
{% endblock %}
