{% extends 'base.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block body %}
<div class="card shadow-sm page-card">
    <div class="card-body p-4">
        <h1 class="h4 mb-3">{{ title|default('Catégorie') }}</h1>

        {{ include('category/_form.html.twig') }}
    </div>
</div>
{% endblock %}
